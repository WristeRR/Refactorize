import React from 'react';
import '../events.json';

export default function Info(props){
    const {event}= props;

    return (
        <div className="info">
            <h2>Venue: {}</h2>
        </div>
    )
}